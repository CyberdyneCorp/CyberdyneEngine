// clang-format off
// NOLINTBEGIN
// Generated by tools/gen/reflect_gen.py. Do not edit.
//
// Source: src/scene/include/cy/scene/components.h
// Regenerate with `just generate-headers`; `just generate-check` fails when this file is stale, and
// `just quality-identity` fails when an identifier in it has moved.
//
// Every identifier below is read from identity/manifest.toml. Nothing here is derived from a name,
// so renaming a type or a field changes the manifest's `name` and this file's strings, and no
// number moves.
//
// clang-format is off for the whole file. Generated output must be byte-identical between runs, and
// running it through a formatter would make that a property of whichever formatter version happened
// to be on the contributor's PATH — `just quality-format-check` and `just generate-check` would then
// disagree about the same file. The emitter owns the layout instead. The marker is the first line
// of the file rather than a later one, because a formatter still reflows everything above it.
//
// clang-tidy is off for the same reason: a finding here is actionable only by editing
// tools/gen/reflect/emit.py, which is where the lint that matters belongs. The compiler's warning
// set still applies, with -Werror, exactly as it does to every other translation unit.


#include <cy/scene/components.reflect.h>

#include <cstddef>
#include <new>
#include <type_traits>
#include <utility>

namespace cy::reflect {
namespace {

// cy::scene::ChildOrder — TypeId 4, declared at src/scene/include/cy/scene/components.h:99.
using Type0 = ::cy::scene::ChildOrder;

static_assert(std::is_standard_layout_v<Type0>,
              "cy::scene::ChildOrder is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type0>,
              "cy::scene::ChildOrder is annotated for reflection and has a vtable.");

void construct_0_(void* storage) {
    ::new (storage) Type0();
}
void destruct_0_(void* object) {
    static_cast<Type0*>(object)->~Type0();
}
void copy_assign_0_(void* destination, const void* source) {
    *static_cast<Type0*>(destination) = *static_cast<const Type0*>(source);
}

constexpr FieldInfo fields_0_[] = {
    FieldInfo{
        .name = "value",
        .id = FieldId{1},
        .kind = FieldKind::U32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type0, value)),
        .size = static_cast<u32>(sizeof(std::declval<Type0&>().value)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Hierarchy"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
};

constexpr TypeInfo type_0_ = TypeInfo{
    .name = "cy::scene::ChildOrder",
    .id = TypeId{4},
    .size = static_cast<u32>(sizeof(Type0)),
    .alignment = static_cast<u32>(alignof(Type0)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type0> &&
                             std::is_trivially_destructible_v<Type0>,
    .construct = &construct_0_,
    .destruct = &destruct_0_,
    .copy_assign = &copy_assign_0_,
    .fields = fields_0_,
    .field_count = 1,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::LocalTransform — TypeId 5, declared at src/scene/include/cy/scene/components.h:111.
using Type1 = ::cy::scene::LocalTransform;

static_assert(std::is_standard_layout_v<Type1>,
              "cy::scene::LocalTransform is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type1>,
              "cy::scene::LocalTransform is annotated for reflection and has a vtable.");

void construct_1_(void* storage) {
    ::new (storage) Type1();
}
void destruct_1_(void* object) {
    static_cast<Type1*>(object)->~Type1();
}
void copy_assign_1_(void* destination, const void* source) {
    *static_cast<Type1*>(destination) = *static_cast<const Type1*>(source);
}

constexpr FieldInfo fields_1_[] = {
    FieldInfo{
        .name = "value.rotation.x",
        .id = FieldId{1},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.rotation.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.rotation.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.rotation.y",
        .id = FieldId{2},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.rotation.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.rotation.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.rotation.z",
        .id = FieldId{3},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.rotation.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.rotation.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.rotation.w",
        .id = FieldId{4},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.rotation.w)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.rotation.w)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.translation.x",
        .id = FieldId{5},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.translation.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.translation.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.translation.y",
        .id = FieldId{6},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.translation.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.translation.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.translation.z",
        .id = FieldId{7},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.translation.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.translation.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.scale.x",
        .id = FieldId{8},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.scale.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.scale.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.scale.y",
        .id = FieldId{9},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.scale.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.scale.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "value.scale.z",
        .id = FieldId{10},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type1, value.scale.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type1&>().value.scale.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
};

constexpr TypeInfo type_1_ = TypeInfo{
    .name = "cy::scene::LocalTransform",
    .id = TypeId{5},
    .size = static_cast<u32>(sizeof(Type1)),
    .alignment = static_cast<u32>(alignof(Type1)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type1> &&
                             std::is_trivially_destructible_v<Type1>,
    .construct = &construct_1_,
    .destruct = &destruct_1_,
    .copy_assign = &copy_assign_1_,
    .fields = fields_1_,
    .field_count = 10,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::WorldTransform — TypeId 9, declared at src/scene/include/cy/scene/components.h:119.
using Type2 = ::cy::scene::WorldTransform;

static_assert(std::is_standard_layout_v<Type2>,
              "cy::scene::WorldTransform is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type2>,
              "cy::scene::WorldTransform is annotated for reflection and has a vtable.");

void construct_2_(void* storage) {
    ::new (storage) Type2();
}
void destruct_2_(void* object) {
    static_cast<Type2*>(object)->~Type2();
}
void copy_assign_2_(void* destination, const void* source) {
    *static_cast<Type2*>(destination) = *static_cast<const Type2*>(source);
}

constexpr FieldInfo fields_2_[] = {
    FieldInfo{
        .name = "value.rotation.x",
        .id = FieldId{1},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.rotation.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.rotation.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.rotation.y",
        .id = FieldId{2},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.rotation.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.rotation.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.rotation.z",
        .id = FieldId{3},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.rotation.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.rotation.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.rotation.w",
        .id = FieldId{4},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.rotation.w)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.rotation.w)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.translation.x",
        .id = FieldId{5},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.translation.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.translation.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.translation.y",
        .id = FieldId{6},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.translation.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.translation.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.translation.z",
        .id = FieldId{7},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.translation.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.translation.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.scale.x",
        .id = FieldId{8},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.scale.x)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.scale.x)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.scale.y",
        .id = FieldId{9},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.scale.y)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.scale.y)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
    FieldInfo{
        .name = "value.scale.z",
        .id = FieldId{10},
        .kind = FieldKind::F32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type2, value.scale.z)),
        .size = static_cast<u32>(sizeof(std::declval<Type2&>().value.scale.z)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Transform"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
};

constexpr TypeInfo type_2_ = TypeInfo{
    .name = "cy::scene::WorldTransform",
    .id = TypeId{9},
    .size = static_cast<u32>(sizeof(Type2)),
    .alignment = static_cast<u32>(alignof(Type2)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type2> &&
                             std::is_trivially_destructible_v<Type2>,
    .construct = &construct_2_,
    .destruct = &destruct_2_,
    .copy_assign = &copy_assign_2_,
    .fields = fields_2_,
    .field_count = 10,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::NodeFlags — TypeId 6, declared at src/scene/include/cy/scene/components.h:172.
using Type3 = ::cy::scene::NodeFlags;

static_assert(std::is_standard_layout_v<Type3>,
              "cy::scene::NodeFlags is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type3>,
              "cy::scene::NodeFlags is annotated for reflection and has a vtable.");

void construct_3_(void* storage) {
    ::new (storage) Type3();
}
void destruct_3_(void* object) {
    static_cast<Type3*>(object)->~Type3();
}
void copy_assign_3_(void* destination, const void* source) {
    *static_cast<Type3*>(destination) = *static_cast<const Type3*>(source);
}

constexpr FieldInfo fields_3_[] = {
    FieldInfo{
        .name = "visible",
        .id = FieldId{1},
        .kind = FieldKind::Bool,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type3, visible)),
        .size = static_cast<u32>(sizeof(std::declval<Type3&>().visible)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Node"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
    FieldInfo{
        .name = "enabled",
        .id = FieldId{2},
        .kind = FieldKind::Bool,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type3, enabled)),
        .size = static_cast<u32>(sizeof(std::declval<Type3&>().enabled)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category | AttributeKind::Persistence,
            .category = TextAttribute{"Node"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Authoring,
        },
    },
};

constexpr TypeInfo type_3_ = TypeInfo{
    .name = "cy::scene::NodeFlags",
    .id = TypeId{6},
    .size = static_cast<u32>(sizeof(Type3)),
    .alignment = static_cast<u32>(alignof(Type3)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type3> &&
                             std::is_trivially_destructible_v<Type3>,
    .construct = &construct_3_,
    .destruct = &destruct_3_,
    .copy_assign = &copy_assign_3_,
    .fields = fields_3_,
    .field_count = 2,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::NodeState — TypeId 7, declared at src/scene/include/cy/scene/components.h:188.
using Type4 = ::cy::scene::NodeState;

static_assert(std::is_standard_layout_v<Type4>,
              "cy::scene::NodeState is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type4>,
              "cy::scene::NodeState is annotated for reflection and has a vtable.");

void construct_4_(void* storage) {
    ::new (storage) Type4();
}
void destruct_4_(void* object) {
    static_cast<Type4*>(object)->~Type4();
}
void copy_assign_4_(void* destination, const void* source) {
    *static_cast<Type4*>(destination) = *static_cast<const Type4*>(source);
}

constexpr Enumerator enumerators_4_0_[] = {
    Enumerator{"None", 0},
    Enumerator{"Transform", 1},
    Enumerator{"SubtreeTransform", 2},
    Enumerator{"Flags", 4},
    Enumerator{"SubtreeFlags", 8},
};

constexpr FieldInfo fields_4_[] = {
    FieldInfo{
        .name = "dirty",
        .id = FieldId{1},
        .kind = FieldKind::U8,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type4, dirty)),
        .size = static_cast<u32>(sizeof(std::declval<Type4&>().dirty)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Flags
                        | AttributeKind::Hidden
                        | AttributeKind::Persistence,
            .enumeration = EnumAttribute{enumerators_4_0_, 5},
            .category = TextAttribute{"Node"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::Derived,
        },
    },
};

constexpr TypeInfo type_4_ = TypeInfo{
    .name = "cy::scene::NodeState",
    .id = TypeId{7},
    .size = static_cast<u32>(sizeof(Type4)),
    .alignment = static_cast<u32>(alignof(Type4)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type4> &&
                             std::is_trivially_destructible_v<Type4>,
    .construct = &construct_4_,
    .destruct = &destruct_4_,
    .copy_assign = &copy_assign_4_,
    .fields = fields_4_,
    .field_count = 1,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::SceneRef — TypeId 8, declared at src/scene/include/cy/scene/components.h:211.
using Type5 = ::cy::scene::SceneRef;

static_assert(std::is_standard_layout_v<Type5>,
              "cy::scene::SceneRef is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type5>,
              "cy::scene::SceneRef is annotated for reflection and has a vtable.");

void construct_5_(void* storage) {
    ::new (storage) Type5();
}
void destruct_5_(void* object) {
    static_cast<Type5*>(object)->~Type5();
}
void copy_assign_5_(void* destination, const void* source) {
    *static_cast<Type5*>(destination) = *static_cast<const Type5*>(source);
}

constexpr FieldInfo fields_5_[] = {
    FieldInfo{
        .name = "scene",
        .id = FieldId{1},
        .kind = FieldKind::U32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type5, scene)),
        .size = static_cast<u32>(sizeof(std::declval<Type5&>().scene)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Scene"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::RuntimeState,
        },
    },
};

constexpr TypeInfo type_5_ = TypeInfo{
    .name = "cy::scene::SceneRef",
    .id = TypeId{8},
    .size = static_cast<u32>(sizeof(Type5)),
    .alignment = static_cast<u32>(alignof(Type5)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type5> &&
                             std::is_trivially_destructible_v<Type5>,
    .construct = &construct_5_,
    .destruct = &destruct_5_,
    .copy_assign = &copy_assign_5_,
    .fields = fields_5_,
    .field_count = 1,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

// cy::scene::BehaviourRef — TypeId 3, declared at src/scene/include/cy/scene/components.h:219.
using Type6 = ::cy::scene::BehaviourRef;

static_assert(std::is_standard_layout_v<Type6>,
              "cy::scene::BehaviourRef is annotated for reflection but is not standard-layout. Reflection "
              "is opt-in and data-oriented: a reflected type has no base class, no virtual "
              "functions and no hidden fields, so that its offsets mean what they say.");
static_assert(!std::is_polymorphic_v<Type6>,
              "cy::scene::BehaviourRef is annotated for reflection and has a vtable.");

void construct_6_(void* storage) {
    ::new (storage) Type6();
}
void destruct_6_(void* object) {
    static_cast<Type6*>(object)->~Type6();
}
void copy_assign_6_(void* destination, const void* source) {
    *static_cast<Type6*>(destination) = *static_cast<const Type6*>(source);
}

constexpr FieldInfo fields_6_[] = {
    FieldInfo{
        .name = "instance",
        .id = FieldId{1},
        .kind = FieldKind::U32,
        .type = TypeId{},
        .offset = static_cast<u32>(offsetof(Type6, instance)),
        .size = static_cast<u32>(sizeof(std::declval<Type6&>().instance)),
        .attributes = FieldAttributes{
            .declared = AttributeKind::Category
                        | AttributeKind::Persistence
                        | AttributeKind::ReadOnly,
            .category = TextAttribute{"Behaviour"},
            .unit = UnitKind::None,
            .persistence = PersistenceKind::RuntimeState,
        },
    },
};

constexpr TypeInfo type_6_ = TypeInfo{
    .name = "cy::scene::BehaviourRef",
    .id = TypeId{3},
    .size = static_cast<u32>(sizeof(Type6)),
    .alignment = static_cast<u32>(alignof(Type6)),
    .trivially_relocatable = std::is_trivially_copyable_v<Type6> &&
                             std::is_trivially_destructible_v<Type6>,
    .construct = &construct_6_,
    .destruct = &destruct_6_,
    .copy_assign = &copy_assign_6_,
    .fields = fields_6_,
    .field_count = 1,
    .methods = nullptr,
    .method_count = 0,
    .header = "src/scene/include/cy/scene/components.h",
    .module = "scene-include",
};

}  // namespace

const TypeInfo& Reflected<::cy::scene::ChildOrder>::info() noexcept {
    return type_0_;
}

const TypeInfo& Reflected<::cy::scene::LocalTransform>::info() noexcept {
    return type_1_;
}

const TypeInfo& Reflected<::cy::scene::WorldTransform>::info() noexcept {
    return type_2_;
}

const TypeInfo& Reflected<::cy::scene::NodeFlags>::info() noexcept {
    return type_3_;
}

const TypeInfo& Reflected<::cy::scene::NodeState>::info() noexcept {
    return type_4_;
}

const TypeInfo& Reflected<::cy::scene::SceneRef>::info() noexcept {
    return type_5_;
}

const TypeInfo& Reflected<::cy::scene::BehaviourRef>::info() noexcept {
    return type_6_;
}

Status register_cy_scene_components(TypeRegistry& registry) {
    if (auto added = registry.add(type_0_); !added) {
        return added;
    }
    if (auto added = registry.add(type_1_); !added) {
        return added;
    }
    if (auto added = registry.add(type_2_); !added) {
        return added;
    }
    if (auto added = registry.add(type_3_); !added) {
        return added;
    }
    if (auto added = registry.add(type_4_); !added) {
        return added;
    }
    if (auto added = registry.add(type_5_); !added) {
        return added;
    }
    if (auto added = registry.add(type_6_); !added) {
        return added;
    }
    return ok();
}

}  // namespace cy::reflect

// NOLINTEND
