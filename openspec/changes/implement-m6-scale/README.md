# implement-m6-scale

M6 of the roadmap: a world larger than memory and a build that is a graph rather than a script — and the milestone at which the editor finally opens a world with something in it
