# audit-closed-milestone-columns

M10 task 6.5, closing `m9:record-matches-plan-history`: four closed milestones' matrix columns
claimed nineteen cells the status record does not support. All nineteen are audited against the
tree; fifteen cells move, four rows are claimed, and the rule that finds the next one is written
into `delivery-roadmap`
