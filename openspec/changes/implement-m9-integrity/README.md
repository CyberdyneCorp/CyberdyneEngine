# implement-m9-integrity

M9 of the roadmap: one command log, read five ways — determinism that is validated rather than
asserted, replay and rollback over a single authoritative log, replication built on the same log,
and the diagnostics that turn a divergence into an artefact somebody can reproduce
