# Tasks

- [x] Record the external iOS spike and its remaining hosted-runner limitation.
- [x] Add device and simulator CMake toolchains with documented invocation.
- [x] Teach module discovery and top-level platform selection about iOS.
- [x] Implement the iOS `Platform`, `DisplayServer`, and touch input source under `platform/ios/`.
- [x] Make the Metal backend build and create a `CAMetalLayer` swapchain on iOS.
- [x] Add a signed iOS sample that renders and reports a device-observable success marker.
- [x] Add contract and regression tests that fail when the iOS port is removed or misreported.
- [x] Build, install, launch, and verify the sample on the connected physical iPhone.
- [x] Document the supported workflow and validate OpenSpec and applicable project gates.
