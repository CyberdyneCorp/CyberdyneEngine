# Tasks

- [x] Add the GTAO horizon search and its host reference.
- [x] Add the filter cascade as the shared denoiser's spatial stage, configured from its table.
- [x] Add `FrameStageDeclaration` and the imported ambient occlusion target to the forward frame and the assembly.
- [x] Apply the term to the ambient term in `cy/frame.slang` and regenerate the frame's committed SPIR-V and MSL.
- [x] Add the setting to the beauty shot, its prepass, and `just capture-ambient-occlusion`.
- [x] Add `unit.rendering_occlusion` and `render.ambient_occlusion`, and prove each frame case red by a mutation.
- [x] Publish the before/after images, update the READMEs and the requirements map, and validate this change.
