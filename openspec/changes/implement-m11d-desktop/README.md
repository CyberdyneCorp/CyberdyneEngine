# implement-m11d-desktop

M11.d of the roadmap: the milestone `delivery-roadmap` actually describes — Metal native and D3D12 to
parity with Vulkan, a native `Platform` and `DisplayServer` replacing SDL3 on one desktop with no
change in `src/core/`, `src/ecs/`, `src/servers/` or `src/scene/`, the desktop half of
`rendering-forward-clustered`, the build and quality gates, and `samples/11-ship` on desktop.
