# implement-m11e-ship

M11.e of the roadmap and the 1.0 gate: mobile targets and cross-compilation, the mobile half of
`rendering-forward-clustered`, the full CI matrix, the four release recipes that refuse today, the
rest of the dependency set, **the editor's material authoring front end inherited from M11.c** beside
the `.cygraph` writing this rung was already assigned (tasks section 5a), and the sweep — every
remaining capability Complete or explicitly deferred with its re-entry point, `xr-support` included.
Ends with the 1.0 record.
