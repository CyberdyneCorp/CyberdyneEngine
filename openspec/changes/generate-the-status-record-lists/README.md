# generate-the-status-record-lists

M9's closing gate, task 7.6: the capability matrix's three status lists were hand-maintained and
three milestones stale. They are now rendered from `docs/roadmap/status.yaml` by
`just roadmap-status`, which fails when they drift
