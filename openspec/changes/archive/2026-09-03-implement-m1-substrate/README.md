# implement-m1-substrate

M1 of the roadmap: reflection and stable field identity, memory domains and containers, math and the coordinate conventions, the job system, the virtual filesystem — closing on a headless host that loads a package, runs a parallel job graph, and reports its memory budget tree
