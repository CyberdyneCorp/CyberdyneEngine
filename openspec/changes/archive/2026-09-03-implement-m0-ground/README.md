# implement-m0-ground

M0 of the roadmap: the repository skeleton, the CMake build with enforced layering, the `justfile` and CI, an SDL3-backed window, the trace and log, and the test and roadmap gates — closing on an application that opens a window and writes a trace
