## REMOVED Requirements

### Requirement: Particle systems

Superseded by the `vfx-system` capability, which replaces the module-pipeline particle system
described here with a compiled-graph, GPU-first VFX framework. The capabilities described in this
requirement — GPU simulation, module composition, sorting, and collision against analytic shapes,
the depth buffer, and a signed distance field — are all preserved and extended there.
