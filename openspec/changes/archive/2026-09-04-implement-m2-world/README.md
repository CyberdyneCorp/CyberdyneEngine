# implement-m2-world

M2 of the roadmap: the archetype ECS, the node façade over it, serialization and prefabs, the fixed-tick loop, and the determinism seeds every later milestone assumes — closing on a headless run that reproduces its state hash exactly
