# add-editor-agent-interface

Agents drive the editor over MCP as a projection of the command registry — every action, every transaction, every gizmo operation, plus a viewport they can actually see, with attribution and undo that work exactly as they do for a human
