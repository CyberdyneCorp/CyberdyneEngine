# Tasks: correct M8's artefact claim

- [x] 1.1 Correct the M8 row's closing artefact in `delivery-roadmap`'s milestone ladder: it names
      what the artefact exercises and names `vfx-system` as the capability that did not reach
      Working, rather than claiming every gameplay-facing capability did
- [x] 1.2 Record M8's three-way split in the same requirement, in the shape M5.5's insertion is
      recorded: what was learned, and why splitting rather than renumbering
- [ ] 1.3 Archive with the change that closes the next milestone, so the specification and the
      status record move together
