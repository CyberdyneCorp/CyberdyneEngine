# tools/ — layer 7: code generators and command-line utilities.
#
# Placeholder. Built when CY_BUILD_TOOLS is on, and never linked into a shipped runtime. The first
# targets here are tools/layercheck/ (task 1.3.3) and tools/deps/ (task 1.6.4); several tools are
# scripts rather than compiled targets and are invoked by recipes directly.
